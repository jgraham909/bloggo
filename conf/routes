# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /                                       Application.Index
GET     /user/index                             User.Index
GET     /user/logout                            User.Logout
GET     /user/login                             User.LoginForm
POST    /user/login                             User.Login
GET		  /user/register							            User.RegisterForm
GET     /blog                                   Blog.Index
GET     /blog/add                               Blog.Add
GET     /blog/:id                               Blog.View
GET     /blog/:id/edit                          Blog.Edit
GET     /blog/:id/links                         Blog.EditLinks
GET     /blog/:id/deleteConfirm                 Blog.DeleteConfirm
GET     /blog/:id/delete                        Blog.Delete
GET     /blog/tag/:t                            Blog.Tag
# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /*filepath                       Static.Serve("public")

# Catch all
*       /:controller/:action                    :controller.:action
